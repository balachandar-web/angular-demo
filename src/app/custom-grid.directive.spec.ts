import { CustomGridDirective } from './custom-grid.directive';

describe('CustomGridDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomGridDirective();
    expect(directive).toBeTruthy();
  });
});
